<h1>Books</h1>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <% @books.each do |book| %>
  <tbody>
    <tr>
      <td><%= link_to book.title, book_path(book) %></td>
      <td><%= link_to book.body, book_path(book) %></td>
      <td>show</td>
      <td>edit</td>
      <td>destroy</td>
    </tr>
  </tbody>
  <% end 
</table>
